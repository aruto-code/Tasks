const createLogger = require('logger');
const config = require('./config');

const logger = createLogger({
  serviceName: config.serviceId,
  env: config.env,
  loki: {
    host: config.loki_url,
    labels: { app: config.serviceId },
  },
});

module.exports = logger;
